---
description: Dockerfile and Compose conventions
globs: "Dockerfile*,**/compose*.yml,**/compose*.yaml,**/docker-compose*.yml,**/docker-compose*.yaml"
alwaysApply: false
---

# Docker files

When these paths are in play, follow `docker-conventions`.
